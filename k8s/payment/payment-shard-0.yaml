apiVersion: apps/v1
kind: Deployment
metadata:
  name: payment-shard-0
  namespace: dds25
spec:
  replicas: 1
  selector:
    matchLabels:
      app: payment-shard-0
  template:
    metadata:
      labels:
        app: payment-shard-0
    spec:
      containers:
        - name: payment
          image: payment:latest
          imagePullPolicy: IfNotPresent
          command: ["/bin/sh", "-c"]
          args:
            - |
              python -m payment.worker &
              exec gunicorn -b 0.0.0.0:5000 -w 2 --timeout 30 --log-level=info app:app
          ports:
            - containerPort: 5000
          env:
            - name: SHARD_INDEX
              value: "0"
            - name: NUM_SHARDS
              value: "3"
            - name: REDIS_HOST
              value: "payment-db-0"
            - name: REDIS_PORT
              value: "6379"
            - name: REDIS_PASSWORD
              value: "redis"
            - name: REDIS_DB
              value: "0"
            - name: KAFKA_BOOTSTRAP
              value: "kafka-0.kafka-headless:9092,kafka-1.kafka-headless:9092,kafka-2.kafka-headless:9092"
            - name: KAFKA_CLIENT_ID
              value: "payment-service-0"
            - name: KAFKA_GROUP_ID
              value: "payment-service-0"
            - name: LOG_LEVEL
              value: "WARNING"
            - name: PYTHONPATH
              value: "/app"
---
apiVersion: v1
kind: Service
metadata:
  name: payment-shard-0
  namespace: dds25
spec:
  selector:
    app: payment-shard-0
  ports:
    - port: 5000
      targetPort: 5000
