apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: order-db-1-pvc
  namespace: dds25
spec:
  accessModes: [ReadWriteOnce]
  resources:
    requests:
      storage: 512Mi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: order-db-1
  namespace: dds25
spec:
  replicas: 1
  selector:
    matchLabels:
      app: order-db-1
  template:
    metadata:
      labels:
        app: order-db-1
    spec:
      containers:
        - name: redis
          image: redis:7.2-bookworm
          args: ["--requirepass", "redis", "--maxmemory", "512mb", "--appendonly", "yes"]
          ports:
            - containerPort: 6379
          volumeMounts:
            - name: data
              mountPath: /data
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: order-db-1-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: order-db-1
  namespace: dds25
spec:
  selector:
    app: order-db-1
  ports:
    - port: 6379
      targetPort: 6379
