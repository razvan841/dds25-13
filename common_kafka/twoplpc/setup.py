# twoplpc package setup for editable install
from setuptools import setup

setup(
    name="twoplpc",
    version="0.1.0",
    packages=["twoplpc"],
    package_dir={"twoplpc": "."},
    install_requires=[],
)
